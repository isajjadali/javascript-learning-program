// Create a function name `printUser` who takes an object of user as argument and print the user name
// Point to be Noted:
// 1. if we pass null as an argument then the function will return 'wrong input'
// 2. if we pass undefined as an argument then the function will return undefined
// 3. must use default paramter

let user = {
    name: 'Sajjad Ali'
}

function printUser() {

}

console.log(printUser());